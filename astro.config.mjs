// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
	// Use your production domain for features like sitemap/OG URLs.
	site: 'https://itkdm.com',
	output: 'static',
});
