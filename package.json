{
  "name": "terrestrial-tower",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "predev": "node scripts/fetch-releases.mjs",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "prebuild": "node scripts/fetch-releases.mjs",
    "postbuild": "npx --yes pagefind --site dist && node scripts/push-baidu.mjs"
  },
  "dependencies": {
    "@astrojs/rss": "^4.0.14",
    "astro": "^5.16.9",
    "marked": "^17.0.1",
    "qrcode": "^1.5.4"
  },
  "devDependencies": {
    "gray-matter": "^4.0.3"
  }
}
